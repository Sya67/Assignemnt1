#include<stdio.h>
int main()
{
const int N = 6;
float data [6] = {1.2, 8.4, 10.8, -0.4, -8.234, 3.1};
int i;
float max = data[0]
for (i = 0; i<N; i++){
    if (data[i] > max)
        max = data[i];
        maxindex = i;

    return max;
}
printf("Largest in given array is %f found at index %d\n", max, i0);

return 0;
}
